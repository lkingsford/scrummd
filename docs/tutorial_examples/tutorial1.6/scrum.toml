[tool.scrummd]
strict = true

[tool.scrummd.scard]
reference_format = "$index - $summary [$status]"

[tool.scrummd.fields]
severity = ["High", "Medium", "Low"]
status = ["Ready For Development", "In Progress", "Done"]
